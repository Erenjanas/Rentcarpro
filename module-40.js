export function component40_1(data = {}) {
  const title = data.title || 'RentCar Modül 40.1';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-40-1">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component40_2(data = {}) {
  const title = data.title || 'RentCar Modül 40.2';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-40-2">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component40_3(data = {}) {
  const title = data.title || 'RentCar Modül 40.3';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-40-3">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component40_4(data = {}) {
  const title = data.title || 'RentCar Modül 40.4';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-40-4">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component40_5(data = {}) {
  const title = data.title || 'RentCar Modül 40.5';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-40-5">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component40_6(data = {}) {
  const title = data.title || 'RentCar Modül 40.6';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-40-6">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component40_7(data = {}) {
  const title = data.title || 'RentCar Modül 40.7';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-40-7">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component40_8(data = {}) {
  const title = data.title || 'RentCar Modül 40.8';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-40-8">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component40_9(data = {}) {
  const title = data.title || 'RentCar Modül 40.9';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-40-9">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component40_10(data = {}) {
  const title = data.title || 'RentCar Modül 40.10';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-40-10">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component40_11(data = {}) {
  const title = data.title || 'RentCar Modül 40.11';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-40-11">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component40_12(data = {}) {
  const title = data.title || 'RentCar Modül 40.12';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-40-12">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component40_13(data = {}) {
  const title = data.title || 'RentCar Modül 40.13';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-40-13">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component40_14(data = {}) {
  const title = data.title || 'RentCar Modül 40.14';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-40-14">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component40_15(data = {}) {
  const title = data.title || 'RentCar Modül 40.15';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-40-15">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component40_16(data = {}) {
  const title = data.title || 'RentCar Modül 40.16';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-40-16">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component40_17(data = {}) {
  const title = data.title || 'RentCar Modül 40.17';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-40-17">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component40_18(data = {}) {
  const title = data.title || 'RentCar Modül 40.18';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-40-18">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component40_19(data = {}) {
  const title = data.title || 'RentCar Modül 40.19';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-40-19">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component40_20(data = {}) {
  const title = data.title || 'RentCar Modül 40.20';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-40-20">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component40_21(data = {}) {
  const title = data.title || 'RentCar Modül 40.21';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-40-21">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component40_22(data = {}) {
  const title = data.title || 'RentCar Modül 40.22';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-40-22">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component40_23(data = {}) {
  const title = data.title || 'RentCar Modül 40.23';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-40-23">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component40_24(data = {}) {
  const title = data.title || 'RentCar Modül 40.24';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-40-24">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component40_25(data = {}) {
  const title = data.title || 'RentCar Modül 40.25';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-40-25">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}
