export function component26_1(data = {}) {
  const title = data.title || 'RentCar Modül 26.1';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-26-1">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component26_2(data = {}) {
  const title = data.title || 'RentCar Modül 26.2';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-26-2">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component26_3(data = {}) {
  const title = data.title || 'RentCar Modül 26.3';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-26-3">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component26_4(data = {}) {
  const title = data.title || 'RentCar Modül 26.4';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-26-4">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component26_5(data = {}) {
  const title = data.title || 'RentCar Modül 26.5';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-26-5">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component26_6(data = {}) {
  const title = data.title || 'RentCar Modül 26.6';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-26-6">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component26_7(data = {}) {
  const title = data.title || 'RentCar Modül 26.7';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-26-7">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component26_8(data = {}) {
  const title = data.title || 'RentCar Modül 26.8';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-26-8">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component26_9(data = {}) {
  const title = data.title || 'RentCar Modül 26.9';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-26-9">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component26_10(data = {}) {
  const title = data.title || 'RentCar Modül 26.10';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-26-10">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component26_11(data = {}) {
  const title = data.title || 'RentCar Modül 26.11';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-26-11">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component26_12(data = {}) {
  const title = data.title || 'RentCar Modül 26.12';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-26-12">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component26_13(data = {}) {
  const title = data.title || 'RentCar Modül 26.13';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-26-13">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component26_14(data = {}) {
  const title = data.title || 'RentCar Modül 26.14';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-26-14">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component26_15(data = {}) {
  const title = data.title || 'RentCar Modül 26.15';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-26-15">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component26_16(data = {}) {
  const title = data.title || 'RentCar Modül 26.16';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-26-16">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component26_17(data = {}) {
  const title = data.title || 'RentCar Modül 26.17';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-26-17">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component26_18(data = {}) {
  const title = data.title || 'RentCar Modül 26.18';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-26-18">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component26_19(data = {}) {
  const title = data.title || 'RentCar Modül 26.19';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-26-19">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component26_20(data = {}) {
  const title = data.title || 'RentCar Modül 26.20';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-26-20">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component26_21(data = {}) {
  const title = data.title || 'RentCar Modül 26.21';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-26-21">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component26_22(data = {}) {
  const title = data.title || 'RentCar Modül 26.22';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-26-22">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component26_23(data = {}) {
  const title = data.title || 'RentCar Modül 26.23';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-26-23">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component26_24(data = {}) {
  const title = data.title || 'RentCar Modül 26.24';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-26-24">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component26_25(data = {}) {
  const title = data.title || 'RentCar Modül 26.25';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-26-25">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}
