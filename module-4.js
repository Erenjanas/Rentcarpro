export function component4_1(data = {}) {
  const title = data.title || 'RentCar Modül 4.1';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-4-1">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component4_2(data = {}) {
  const title = data.title || 'RentCar Modül 4.2';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-4-2">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component4_3(data = {}) {
  const title = data.title || 'RentCar Modül 4.3';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-4-3">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component4_4(data = {}) {
  const title = data.title || 'RentCar Modül 4.4';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-4-4">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component4_5(data = {}) {
  const title = data.title || 'RentCar Modül 4.5';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-4-5">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component4_6(data = {}) {
  const title = data.title || 'RentCar Modül 4.6';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-4-6">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component4_7(data = {}) {
  const title = data.title || 'RentCar Modül 4.7';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-4-7">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component4_8(data = {}) {
  const title = data.title || 'RentCar Modül 4.8';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-4-8">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component4_9(data = {}) {
  const title = data.title || 'RentCar Modül 4.9';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-4-9">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component4_10(data = {}) {
  const title = data.title || 'RentCar Modül 4.10';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-4-10">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component4_11(data = {}) {
  const title = data.title || 'RentCar Modül 4.11';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-4-11">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component4_12(data = {}) {
  const title = data.title || 'RentCar Modül 4.12';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-4-12">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component4_13(data = {}) {
  const title = data.title || 'RentCar Modül 4.13';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-4-13">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component4_14(data = {}) {
  const title = data.title || 'RentCar Modül 4.14';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-4-14">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component4_15(data = {}) {
  const title = data.title || 'RentCar Modül 4.15';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-4-15">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component4_16(data = {}) {
  const title = data.title || 'RentCar Modül 4.16';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-4-16">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component4_17(data = {}) {
  const title = data.title || 'RentCar Modül 4.17';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-4-17">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component4_18(data = {}) {
  const title = data.title || 'RentCar Modül 4.18';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-4-18">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component4_19(data = {}) {
  const title = data.title || 'RentCar Modül 4.19';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-4-19">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component4_20(data = {}) {
  const title = data.title || 'RentCar Modül 4.20';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-4-20">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component4_21(data = {}) {
  const title = data.title || 'RentCar Modül 4.21';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-4-21">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component4_22(data = {}) {
  const title = data.title || 'RentCar Modül 4.22';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-4-22">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component4_23(data = {}) {
  const title = data.title || 'RentCar Modül 4.23';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-4-23">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component4_24(data = {}) {
  const title = data.title || 'RentCar Modül 4.24';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-4-24">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component4_25(data = {}) {
  const title = data.title || 'RentCar Modül 4.25';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-4-25">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}
