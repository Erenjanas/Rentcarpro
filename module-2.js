export function component2_1(data = {}) {
  const title = data.title || 'RentCar Modül 2.1';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-2-1">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component2_2(data = {}) {
  const title = data.title || 'RentCar Modül 2.2';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-2-2">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component2_3(data = {}) {
  const title = data.title || 'RentCar Modül 2.3';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-2-3">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component2_4(data = {}) {
  const title = data.title || 'RentCar Modül 2.4';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-2-4">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component2_5(data = {}) {
  const title = data.title || 'RentCar Modül 2.5';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-2-5">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component2_6(data = {}) {
  const title = data.title || 'RentCar Modül 2.6';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-2-6">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component2_7(data = {}) {
  const title = data.title || 'RentCar Modül 2.7';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-2-7">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component2_8(data = {}) {
  const title = data.title || 'RentCar Modül 2.8';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-2-8">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component2_9(data = {}) {
  const title = data.title || 'RentCar Modül 2.9';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-2-9">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component2_10(data = {}) {
  const title = data.title || 'RentCar Modül 2.10';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-2-10">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component2_11(data = {}) {
  const title = data.title || 'RentCar Modül 2.11';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-2-11">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component2_12(data = {}) {
  const title = data.title || 'RentCar Modül 2.12';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-2-12">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component2_13(data = {}) {
  const title = data.title || 'RentCar Modül 2.13';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-2-13">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component2_14(data = {}) {
  const title = data.title || 'RentCar Modül 2.14';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-2-14">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component2_15(data = {}) {
  const title = data.title || 'RentCar Modül 2.15';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-2-15">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component2_16(data = {}) {
  const title = data.title || 'RentCar Modül 2.16';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-2-16">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component2_17(data = {}) {
  const title = data.title || 'RentCar Modül 2.17';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-2-17">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component2_18(data = {}) {
  const title = data.title || 'RentCar Modül 2.18';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-2-18">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component2_19(data = {}) {
  const title = data.title || 'RentCar Modül 2.19';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-2-19">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component2_20(data = {}) {
  const title = data.title || 'RentCar Modül 2.20';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-2-20">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component2_21(data = {}) {
  const title = data.title || 'RentCar Modül 2.21';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-2-21">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component2_22(data = {}) {
  const title = data.title || 'RentCar Modül 2.22';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-2-22">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component2_23(data = {}) {
  const title = data.title || 'RentCar Modül 2.23';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-2-23">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component2_24(data = {}) {
  const title = data.title || 'RentCar Modül 2.24';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-2-24">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component2_25(data = {}) {
  const title = data.title || 'RentCar Modül 2.25';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-2-25">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}
