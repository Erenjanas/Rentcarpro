export function component25_1(data = {}) {
  const title = data.title || 'RentCar Modül 25.1';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-25-1">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component25_2(data = {}) {
  const title = data.title || 'RentCar Modül 25.2';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-25-2">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component25_3(data = {}) {
  const title = data.title || 'RentCar Modül 25.3';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-25-3">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component25_4(data = {}) {
  const title = data.title || 'RentCar Modül 25.4';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-25-4">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component25_5(data = {}) {
  const title = data.title || 'RentCar Modül 25.5';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-25-5">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component25_6(data = {}) {
  const title = data.title || 'RentCar Modül 25.6';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-25-6">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component25_7(data = {}) {
  const title = data.title || 'RentCar Modül 25.7';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-25-7">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component25_8(data = {}) {
  const title = data.title || 'RentCar Modül 25.8';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-25-8">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component25_9(data = {}) {
  const title = data.title || 'RentCar Modül 25.9';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-25-9">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component25_10(data = {}) {
  const title = data.title || 'RentCar Modül 25.10';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-25-10">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component25_11(data = {}) {
  const title = data.title || 'RentCar Modül 25.11';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-25-11">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component25_12(data = {}) {
  const title = data.title || 'RentCar Modül 25.12';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-25-12">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component25_13(data = {}) {
  const title = data.title || 'RentCar Modül 25.13';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-25-13">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component25_14(data = {}) {
  const title = data.title || 'RentCar Modül 25.14';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-25-14">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component25_15(data = {}) {
  const title = data.title || 'RentCar Modül 25.15';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-25-15">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component25_16(data = {}) {
  const title = data.title || 'RentCar Modül 25.16';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-25-16">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component25_17(data = {}) {
  const title = data.title || 'RentCar Modül 25.17';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-25-17">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component25_18(data = {}) {
  const title = data.title || 'RentCar Modül 25.18';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-25-18">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component25_19(data = {}) {
  const title = data.title || 'RentCar Modül 25.19';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-25-19">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component25_20(data = {}) {
  const title = data.title || 'RentCar Modül 25.20';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-25-20">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component25_21(data = {}) {
  const title = data.title || 'RentCar Modül 25.21';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-25-21">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component25_22(data = {}) {
  const title = data.title || 'RentCar Modül 25.22';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-25-22">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component25_23(data = {}) {
  const title = data.title || 'RentCar Modül 25.23';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-25-23">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component25_24(data = {}) {
  const title = data.title || 'RentCar Modül 25.24';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-25-24">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component25_25(data = {}) {
  const title = data.title || 'RentCar Modül 25.25';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-25-25">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}
