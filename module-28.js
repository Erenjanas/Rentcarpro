export function component28_1(data = {}) {
  const title = data.title || 'RentCar Modül 28.1';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-28-1">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component28_2(data = {}) {
  const title = data.title || 'RentCar Modül 28.2';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-28-2">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component28_3(data = {}) {
  const title = data.title || 'RentCar Modül 28.3';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-28-3">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component28_4(data = {}) {
  const title = data.title || 'RentCar Modül 28.4';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-28-4">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component28_5(data = {}) {
  const title = data.title || 'RentCar Modül 28.5';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-28-5">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component28_6(data = {}) {
  const title = data.title || 'RentCar Modül 28.6';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-28-6">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component28_7(data = {}) {
  const title = data.title || 'RentCar Modül 28.7';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-28-7">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component28_8(data = {}) {
  const title = data.title || 'RentCar Modül 28.8';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-28-8">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component28_9(data = {}) {
  const title = data.title || 'RentCar Modül 28.9';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-28-9">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component28_10(data = {}) {
  const title = data.title || 'RentCar Modül 28.10';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-28-10">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component28_11(data = {}) {
  const title = data.title || 'RentCar Modül 28.11';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-28-11">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component28_12(data = {}) {
  const title = data.title || 'RentCar Modül 28.12';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-28-12">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component28_13(data = {}) {
  const title = data.title || 'RentCar Modül 28.13';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-28-13">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component28_14(data = {}) {
  const title = data.title || 'RentCar Modül 28.14';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-28-14">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component28_15(data = {}) {
  const title = data.title || 'RentCar Modül 28.15';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-28-15">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component28_16(data = {}) {
  const title = data.title || 'RentCar Modül 28.16';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-28-16">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component28_17(data = {}) {
  const title = data.title || 'RentCar Modül 28.17';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-28-17">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component28_18(data = {}) {
  const title = data.title || 'RentCar Modül 28.18';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-28-18">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component28_19(data = {}) {
  const title = data.title || 'RentCar Modül 28.19';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-28-19">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component28_20(data = {}) {
  const title = data.title || 'RentCar Modül 28.20';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-28-20">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component28_21(data = {}) {
  const title = data.title || 'RentCar Modül 28.21';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-28-21">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component28_22(data = {}) {
  const title = data.title || 'RentCar Modül 28.22';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-28-22">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component28_23(data = {}) {
  const title = data.title || 'RentCar Modül 28.23';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-28-23">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component28_24(data = {}) {
  const title = data.title || 'RentCar Modül 28.24';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-28-24">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component28_25(data = {}) {
  const title = data.title || 'RentCar Modül 28.25';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-28-25">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}
