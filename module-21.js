export function component21_1(data = {}) {
  const title = data.title || 'RentCar Modül 21.1';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-21-1">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component21_2(data = {}) {
  const title = data.title || 'RentCar Modül 21.2';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-21-2">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component21_3(data = {}) {
  const title = data.title || 'RentCar Modül 21.3';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-21-3">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component21_4(data = {}) {
  const title = data.title || 'RentCar Modül 21.4';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-21-4">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component21_5(data = {}) {
  const title = data.title || 'RentCar Modül 21.5';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-21-5">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component21_6(data = {}) {
  const title = data.title || 'RentCar Modül 21.6';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-21-6">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component21_7(data = {}) {
  const title = data.title || 'RentCar Modül 21.7';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-21-7">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component21_8(data = {}) {
  const title = data.title || 'RentCar Modül 21.8';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-21-8">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component21_9(data = {}) {
  const title = data.title || 'RentCar Modül 21.9';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-21-9">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component21_10(data = {}) {
  const title = data.title || 'RentCar Modül 21.10';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-21-10">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component21_11(data = {}) {
  const title = data.title || 'RentCar Modül 21.11';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-21-11">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component21_12(data = {}) {
  const title = data.title || 'RentCar Modül 21.12';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-21-12">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component21_13(data = {}) {
  const title = data.title || 'RentCar Modül 21.13';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-21-13">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component21_14(data = {}) {
  const title = data.title || 'RentCar Modül 21.14';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-21-14">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component21_15(data = {}) {
  const title = data.title || 'RentCar Modül 21.15';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-21-15">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component21_16(data = {}) {
  const title = data.title || 'RentCar Modül 21.16';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-21-16">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component21_17(data = {}) {
  const title = data.title || 'RentCar Modül 21.17';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-21-17">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component21_18(data = {}) {
  const title = data.title || 'RentCar Modül 21.18';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-21-18">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component21_19(data = {}) {
  const title = data.title || 'RentCar Modül 21.19';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-21-19">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component21_20(data = {}) {
  const title = data.title || 'RentCar Modül 21.20';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-21-20">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component21_21(data = {}) {
  const title = data.title || 'RentCar Modül 21.21';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-21-21">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component21_22(data = {}) {
  const title = data.title || 'RentCar Modül 21.22';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-21-22">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component21_23(data = {}) {
  const title = data.title || 'RentCar Modül 21.23';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-21-23">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component21_24(data = {}) {
  const title = data.title || 'RentCar Modül 21.24';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-21-24">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component21_25(data = {}) {
  const title = data.title || 'RentCar Modül 21.25';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-21-25">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}
