export function component52_1(data = {}) {
  const title = data.title || 'RentCar Modül 52.1';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-52-1">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component52_2(data = {}) {
  const title = data.title || 'RentCar Modül 52.2';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-52-2">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component52_3(data = {}) {
  const title = data.title || 'RentCar Modül 52.3';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-52-3">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component52_4(data = {}) {
  const title = data.title || 'RentCar Modül 52.4';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-52-4">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component52_5(data = {}) {
  const title = data.title || 'RentCar Modül 52.5';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-52-5">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component52_6(data = {}) {
  const title = data.title || 'RentCar Modül 52.6';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-52-6">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component52_7(data = {}) {
  const title = data.title || 'RentCar Modül 52.7';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-52-7">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component52_8(data = {}) {
  const title = data.title || 'RentCar Modül 52.8';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-52-8">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component52_9(data = {}) {
  const title = data.title || 'RentCar Modül 52.9';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-52-9">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component52_10(data = {}) {
  const title = data.title || 'RentCar Modül 52.10';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-52-10">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component52_11(data = {}) {
  const title = data.title || 'RentCar Modül 52.11';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-52-11">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component52_12(data = {}) {
  const title = data.title || 'RentCar Modül 52.12';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-52-12">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component52_13(data = {}) {
  const title = data.title || 'RentCar Modül 52.13';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-52-13">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component52_14(data = {}) {
  const title = data.title || 'RentCar Modül 52.14';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-52-14">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component52_15(data = {}) {
  const title = data.title || 'RentCar Modül 52.15';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-52-15">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component52_16(data = {}) {
  const title = data.title || 'RentCar Modül 52.16';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-52-16">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component52_17(data = {}) {
  const title = data.title || 'RentCar Modül 52.17';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-52-17">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component52_18(data = {}) {
  const title = data.title || 'RentCar Modül 52.18';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-52-18">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component52_19(data = {}) {
  const title = data.title || 'RentCar Modül 52.19';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-52-19">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component52_20(data = {}) {
  const title = data.title || 'RentCar Modül 52.20';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-52-20">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component52_21(data = {}) {
  const title = data.title || 'RentCar Modül 52.21';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-52-21">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component52_22(data = {}) {
  const title = data.title || 'RentCar Modül 52.22';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-52-22">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component52_23(data = {}) {
  const title = data.title || 'RentCar Modül 52.23';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-52-23">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component52_24(data = {}) {
  const title = data.title || 'RentCar Modül 52.24';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-52-24">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component52_25(data = {}) {
  const title = data.title || 'RentCar Modül 52.25';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-52-25">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}
