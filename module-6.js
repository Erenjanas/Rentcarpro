export function component6_1(data = {}) {
  const title = data.title || 'RentCar Modül 6.1';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-6-1">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component6_2(data = {}) {
  const title = data.title || 'RentCar Modül 6.2';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-6-2">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component6_3(data = {}) {
  const title = data.title || 'RentCar Modül 6.3';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-6-3">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component6_4(data = {}) {
  const title = data.title || 'RentCar Modül 6.4';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-6-4">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component6_5(data = {}) {
  const title = data.title || 'RentCar Modül 6.5';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-6-5">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component6_6(data = {}) {
  const title = data.title || 'RentCar Modül 6.6';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-6-6">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component6_7(data = {}) {
  const title = data.title || 'RentCar Modül 6.7';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-6-7">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component6_8(data = {}) {
  const title = data.title || 'RentCar Modül 6.8';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-6-8">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component6_9(data = {}) {
  const title = data.title || 'RentCar Modül 6.9';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-6-9">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component6_10(data = {}) {
  const title = data.title || 'RentCar Modül 6.10';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-6-10">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component6_11(data = {}) {
  const title = data.title || 'RentCar Modül 6.11';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-6-11">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component6_12(data = {}) {
  const title = data.title || 'RentCar Modül 6.12';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-6-12">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component6_13(data = {}) {
  const title = data.title || 'RentCar Modül 6.13';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-6-13">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component6_14(data = {}) {
  const title = data.title || 'RentCar Modül 6.14';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-6-14">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component6_15(data = {}) {
  const title = data.title || 'RentCar Modül 6.15';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-6-15">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component6_16(data = {}) {
  const title = data.title || 'RentCar Modül 6.16';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-6-16">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component6_17(data = {}) {
  const title = data.title || 'RentCar Modül 6.17';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-6-17">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component6_18(data = {}) {
  const title = data.title || 'RentCar Modül 6.18';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-6-18">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component6_19(data = {}) {
  const title = data.title || 'RentCar Modül 6.19';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-6-19">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component6_20(data = {}) {
  const title = data.title || 'RentCar Modül 6.20';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-6-20">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component6_21(data = {}) {
  const title = data.title || 'RentCar Modül 6.21';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-6-21">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component6_22(data = {}) {
  const title = data.title || 'RentCar Modül 6.22';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-6-22">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component6_23(data = {}) {
  const title = data.title || 'RentCar Modül 6.23';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-6-23">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component6_24(data = {}) {
  const title = data.title || 'RentCar Modül 6.24';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-6-24">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component6_25(data = {}) {
  const title = data.title || 'RentCar Modül 6.25';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-6-25">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}
