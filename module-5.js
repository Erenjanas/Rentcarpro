export function component5_1(data = {}) {
  const title = data.title || 'RentCar Modül 5.1';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-5-1">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component5_2(data = {}) {
  const title = data.title || 'RentCar Modül 5.2';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-5-2">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component5_3(data = {}) {
  const title = data.title || 'RentCar Modül 5.3';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-5-3">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component5_4(data = {}) {
  const title = data.title || 'RentCar Modül 5.4';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-5-4">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component5_5(data = {}) {
  const title = data.title || 'RentCar Modül 5.5';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-5-5">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component5_6(data = {}) {
  const title = data.title || 'RentCar Modül 5.6';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-5-6">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component5_7(data = {}) {
  const title = data.title || 'RentCar Modül 5.7';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-5-7">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component5_8(data = {}) {
  const title = data.title || 'RentCar Modül 5.8';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-5-8">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component5_9(data = {}) {
  const title = data.title || 'RentCar Modül 5.9';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-5-9">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component5_10(data = {}) {
  const title = data.title || 'RentCar Modül 5.10';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-5-10">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component5_11(data = {}) {
  const title = data.title || 'RentCar Modül 5.11';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-5-11">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component5_12(data = {}) {
  const title = data.title || 'RentCar Modül 5.12';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-5-12">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component5_13(data = {}) {
  const title = data.title || 'RentCar Modül 5.13';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-5-13">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component5_14(data = {}) {
  const title = data.title || 'RentCar Modül 5.14';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-5-14">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component5_15(data = {}) {
  const title = data.title || 'RentCar Modül 5.15';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-5-15">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component5_16(data = {}) {
  const title = data.title || 'RentCar Modül 5.16';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-5-16">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component5_17(data = {}) {
  const title = data.title || 'RentCar Modül 5.17';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-5-17">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component5_18(data = {}) {
  const title = data.title || 'RentCar Modül 5.18';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-5-18">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component5_19(data = {}) {
  const title = data.title || 'RentCar Modül 5.19';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-5-19">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component5_20(data = {}) {
  const title = data.title || 'RentCar Modül 5.20';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-5-20">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component5_21(data = {}) {
  const title = data.title || 'RentCar Modül 5.21';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-5-21">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component5_22(data = {}) {
  const title = data.title || 'RentCar Modül 5.22';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-5-22">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component5_23(data = {}) {
  const title = data.title || 'RentCar Modül 5.23';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-5-23">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component5_24(data = {}) {
  const title = data.title || 'RentCar Modül 5.24';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-5-24">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}

export function component5_25(data = {}) {
  const title = data.title || 'RentCar Modül 5.25';
  const text = data.text || 'Premium araç kiralama bileşeni';
  return `<section class="module module-5-25">
    <div class="module-head"><h3>${title}</h3><p>${text}</p></div>
    <div class="module-body">Araç, rezervasyon, kampanya ve müşteri deneyimi için hazır UI bloğu.</div>
  </section>`;
}
